# 통합 로드맵 보고서 v3 — 챗봇 베타 우선 + 과제 2 연계 + 회사 확장 권고

> **작성:** 기획자 (검토자 관점)
> **작성일:** 2026-05-11 (rev. 현재 개발 현황 반영)
> **기반 상태:** 알파 종료 보정 + UX 보정 완료, 베타 Stage 1~2 반영, OCR/True Hybrid 샘플 검증 진행, pytest 188 통과
> **참고:** [18_REVIEW_CHANGE_SPEC.md](./18_REVIEW_CHANGE_SPEC.md) · [19_PROJECT_STATUS_SUMMARY.md](./19_PROJECT_STATUS_SUMMARY.md) · [27_BETA_STAGE1_REPORT.md](./27_BETA_STAGE1_REPORT.md) · [29_BETA_STAGE2_REPORT.md](./29_BETA_STAGE2_REPORT.md) · [35_OCR_PIPELINE_REPORT.md](./35_OCR_PIPELINE_REPORT.md) · [44_NATIVE_TABLE_VERIFY_RERUN_REPORT.md](./44_NATIVE_TABLE_VERIFY_RERUN_REPORT.md)

---

## 1. 프로젝트 컨텍스트

본 보고서는 **신한EZ손해보험 사내 캡스톤 프로젝트** 컨텍스트에서 작성된다. 다음 전제를 모든 결정에 반영한다.

| 항목           | 내용                                                              |
| -------------- | ----------------------------------------------------------------- |
| 운영 주체      | 신한EZ손해보험 단일 보험사                                        |
| 사용자         | 사내 직원 전용 (대외 서비스 아님)                                 |
| 데이터 범위    | 자사 약관 중심 +**타사 약관 혼재 가능** (보상 비교·참조용) |
| 캡스톤 팀 범위 | **베타 버전 배포까지**                                      |
| 그 이후        | 회사 측에서 정식 운영·확장(현 보고서 9장에 권고만)               |
| 우선순위       | **과제 1(챗봇) 베타 완성 → 과제 2(자동 계산) 시작**        |

이 전제로 인해 (a) 멀티테넌시·외부 노출·고가용성 같은 정식 운영 항목은 캡스톤 범위 외, (b) 타사 약관도 인덱싱·검색은 가능하되 사내 정책에 따라 표시될 수 있어야 한다, (c) 보험금 자동 계산은 챗봇 베타 배포 후의 후속 트랙이다.

---

## 2. 알파 종료 가능 여부 판정

### 2.1 알파 범위 충족 검사

| 영역           | 알파 요건                             | 현재 상태                                                             | 판정      |
| -------------- | ------------------------------------- | --------------------------------------------------------------------- | --------- |
| RAG 파이프라인 | Dense + BM25 + RRF + LLM              | BGE-M3 + Chroma + BM25 + RRF + reranker + LLM, 단계별 DebugInfo        | 초과 달성 |
| 출처 인용      | 답변 + 페이지·조항                   | 답변 + 자동 첨부 + 인용 출처 중심 표시 + UI expander + PDF 미리보기    | 초과 달성 |
| Streamlit UI   | 챗 + 출처                             | 일반/퀵 코드/약관 정형 모드 + 사용자별 채팅 영속화 + 멀티 채팅 사이드바 | 초과 달성 |
| 인증·로깅     | 단일 비밀번호                         | 역할 기반 인증, 사용자별 감사 로그, 관리자 5탭(검색 진단 포함)         | 초과 달성 |
| LLM 선택       | 로컬 LLM                              | Ollama + OpenAI(gpt-5.x) 그룹별 선택, pro 계열 후보 제외 정책          | 초과 달성 |
| 배포           | 로컬 단일 사용자                      | Streamlit Cloud + 로컬 듀얼, Release assets 부트스트랩                 | 초과 달성 |
| 평가           | recall@8 ≥ 0.7, 페이지 정확도 ≥ 0.6 | M10 평가 + smoke_qa v2 보강                                           | 충족      |
| 테스트         | smoke_qa 통과                         | 최신 전체 회귀 pytest 188건 통과                                      | 충족      |

### 2.2 잔여 보정 현황 (2026-05-11 기준)

1. 출처 첨부 정확도 — 인용 문서 중심 표시로 1차 보정 완료
2. 검색 디버그 로깅 — Dense/BM25/RRF/Rerank 단계별 관리자 진단 탭 구현 완료
3. 정형 시나리오 평가 — smoke_qa v2 추가 완료
4. Cloud 로그 노이즈 — Streamlit 로그 축소 및 부팅 확인 완료
5. UX 보정 — 검색 진단 상시 수집, 물결표 렌더링 방지, 계정별 채팅 영속화/멀티 채팅 완료
6. 남은 운영 리스크 — Streamlit Cloud 파일시스템 휘발성, 사내 자료 클라우드 반영 정책, 100개+ 약관 투입 시 메타 필터 재설계

### 2.3 검토자 결론

> **알파 종료 보정은 완료되었고 베타 트랙에 진입했다.** 현재는 베타 Stage 2까지 반영되어 D3·D4 자사 약관 인덱싱, 비급여 표준 모델 SQLite 적재, OCR 샘플 검증이 진행된 상태다. 다만 클라우드 환경에서는 원본 파일 부재와 보안 정책 때문에 문서별 사이드바 필터가 일단 제거되어, 메타 필터/약관 비교 모드는 후속 베타 항목으로 재설계가 필요하다.

---

## 3. 과제 2와의 관계 분석

### 3.1 두 과제 본질 비교

| 측면      | 과제 1 (현재)      | 과제 2 (예정)                                                 |
| --------- | ------------------ | ------------------------------------------------------------- |
| 입력      | 자연어 질문        | 영수증·진단서·세부내역서 (스캔·이미지,**비정형 표**) |
| 출력      | 텍스트 답변 + 출처 | 면·부책 판정 +**정형 데이터프레임** + 보험금 계산서    |
| 핵심 기술 | RAG · LLM         | OCR ·**비정형 표 정규화** · 코드 매핑 · 룰 엔진      |
| 판단 근거 | 약관 조항          | 약관 조항 + EDI 코드 매핑                                     |

### 3.2 핵심 통찰

> 과제 2의 **면·부책 판정**은 결국 "이 EDI 코드 항목이 약관상 보상 대상인가"를 묻는다. 이는 과제 1의 **약관 정형 검색(보상 판정 모드)**과 같은 질문이다.

따라서 두 과제는 별도 시스템이 아니라 **같은 플랫폼의 두 진입점**으로 가는 것이 맞다. 과제 1의 RAG/Graph가 *판단 엔진*, 과제 2가 *자동화 워크플로우*가 된다.

### 3.3 통합 아키텍처 옵션

| 옵션                          | 구조                                       | 적합성(캡스톤 범위)               |
| ----------------------------- | ------------------------------------------ | --------------------------------- |
| A — 별도 앱 + REST           | 인덱스·인증 이중화, 도메인 분리           | 낮음 (캡스톤 일정·인력에 비효율) |
| B — 단일 플랫폼 페이지 분기  | [챗봇][보험금 계산] 두 페이지, 공유 인프라 | **권장**                    |
| C — 마이크로서비스 + Gateway | 정식 운영 단계 구조                        | 캡스톤 범위 외 (Phase E 권고)     |

→ 옵션 B로 베타까지 완주. 옵션 C 전환은 회사 측 정식 운영 단계 권고 사항.

---

## 4. 과제 1 업데이트 — OCR · 메타 · 그래프

### 4.1 스캔본 PDF + 표·이미지 + OCR

베타 단계에서 추가 투입된 문서 풀에는 **스캔본·이미지 PDF·복합 레이아웃** 표가 섞여 있다. D6 `Claim 실무종합가이드`와 D7 `소비자 상담 주요 사례집`은 텍스트 0% 이미지 PDF로 확인되었고, OCR 없이는 검색 인덱싱이 불가하다.

#### 4.1.1 OCR 도구 비교 (단일 회사·사내 사용 기준)

| 옵션                                     | 한국어 정확도 | 표/레이아웃    | 비용          | 적합 영역                    |
| ---------------------------------------- | ------------- | -------------- | ------------- | ---------------------------- |
| Tesseract                                | 현재 미검증   | 약함           | 무료          | 시스템 패키지 설치 후 재검증 |
| EasyOCR                                  | 샘플 양호     | 약함           | 무료          | 스캔본문 OCR 폴백 후보       |
| PaddleOCR + PP-Structure                 | 레이아웃 양호 | 표 bbox 강함   | 무료          | 표/figure 영역 탐지 1차      |
| Naver CLOVA OCR                          | 샘플 우수     | native table 반환 확인 | 월 1만건 무료 | 표 텍스트·영수증·진단서 권장 |
| Upstage Document AI / Google Document AI | 매우 높음     | 매우 강함      | 유료          | 회사 정식 운영 단계          |

**약관 OCR 전략 (현재 갱신)**: PP-Structure로 레이아웃/표/figure 영역을 잡고, CLOVA native table 또는 True Hybrid 경로로 한글 표 텍스트를 보강한다. PP-Structure 단독은 한글 표 텍스트 품질이 낮았으므로, 베타에서는 "영역 탐지(PP-Structure) + 텍스트/표 인식(CLOVA) + 후처리" 조합을 우선한다.

#### 4.1.2 다중 콘텐츠 타입 보존 인덱싱

스캔본 약관에서 추출되는 콘텐츠는 단순 텍스트가 아니다. 청크 메타에 콘텐츠 타입을 명시해 검색 후 적절히 표현한다.

| 콘텐츠 타입    | 저장 형식                                              | UI 표현                               |
| -------------- | ------------------------------------------------------ | ------------------------------------- |
| 본문 텍스트    | 일반 텍스트 청크                                       | 기존과 동일                           |
| 표             | **JSON(헤더+행) + 텍스트 직렬화 청크 동시 저장** | UI에 표 형식 + 원문 텍스트 둘 다 노출 |
| 이미지/도식    | 이미지 파일 + 캡션·위치 텍스트 청크                   | 출처 expander에 이미지 미리보기       |
| 수식/특수 기호 | 텍스트 청크 + raw 보존                                 | 그대로 노출                           |

청크 메타 추가 필드: `content_type ∈ {"text","table","image","formula"}`, `source_method ∈ {"native","ocr_paddle","ocr_clova"}`, `confidence` (OCR 신뢰도).

표는 검색 시 `text` 직렬화로 매칭하고, UI 렌더 시 `json` 형식으로 다시 표시. 이렇게 두 표현을 함께 두면 BM25/임베딩 검색은 가능하면서도 사용자 가독성을 잃지 않는다.

### 4.2 메타데이터 강화 (100개+ 약관 대비)

현재 메타 위에 다음 필드는 코드에 1차 반영되었다.

- `insurance_company` (자사=신한EZ / 타사명)
- `is_own_company`: bool (자사 약관 여부 — UI에서 자사·타사 분리 표시 가능)
- `product_name` / `product_type` (건강·실손·상해·해외여행·장기치료)
- `effective_date` / `version` (개정 이력 추적)
- `coverage_category` (질병급여·질병비급여·3대비급여·상해급여·상해비급여·입원·외래·약제)
- `clause_type` (보상하는 사항·보상하지 않는 사항·면책·정의·기타)

UI/검색 개선:

- 사이드바에 자사·타사 토글, 상품·시행일 범위 필터
- 자동 라우팅: 질의에 회사명·상품명 포함 시 해당 약관만 우선 검색
- **약관 비교 모드**: 동일 보장종목을 자사·타사 N개 약관에서 동시 검색 (자사 직원이 경쟁 상품과의 차이 파악)

진행 상태:

- `PdfSource`와 청크 메타의 확장 필드 추가 완료
- D3 `자사_SOL건강`, D4 `자사_SOL운전자` 등록 및 인덱싱 완료
- 사이드바 자사·타사/상품 필터는 구현 후 Streamlit Cloud 원본 파일 부재 이슈로 일단 제거
- 메타 필터는 Chroma 인덱스 메타 기반으로 재도입해야 하며, 파일 존재 여부에 의존하면 안 됨

### 4.3 GraphDB 도입 필요성 평가 (베타 안에서 게이트로)

> **결정 정책**: GraphDB·GraphRAG는 *베타 완성을 위해 필수인지*를 평가한 후 도입한다. 메타 강화 + 강화된 RAG로 베타 목표(약관 100개+ 검색·약관 비교·면·부책 판정 정확도)가 충족되면 도입은 회사 측 정식 운영 단계로 미룬다.

평가 기준 (Phase B 종료 시점에 확인):

| 신호                                                                                 | 도입 권장  | 비도입 권장     |
| ------------------------------------------------------------------------------------ | ---------- | --------------- |
| 다중 hop 추론 필요 (예: "자사 A 상품의 보장이 타사 B 상품의 어떤 조항과 충돌하는가") | 자주 발생  | 거의 발생 안 함 |
| 보장종목·코드·면책 간 관계가 RAG 답변에서 누락                                     | 빈번       | 드묾            |
| 약관 비교 모드가 텍스트 검색만으로 충분                                              | —         | 충분            |
| 100개+ 약관에서 검색 노이즈가 메타 필터로 해결 안 됨                                 | 해결 안 됨 | 해결됨          |

→ 권장 시 베타 마지막 마일스톤(M25)에서 **PoC 수준 GraphRAG**(20~50개 약관 한정, Neo4j 무료 또는 NetworkX 인메모리)로 도입. 본격 운영(Neo4j 클러스터·전체 약관 그래프)은 회사 확장 단계.

---

## 5. 통합 로드맵 (Phase A → D, 캡스톤 범위)

> **재배치 핵심**: 챗봇 베타 완성(Phase B + Phase C)이 과제 2(Phase D)보다 먼저 끝난다.
> **2026-05-07 갱신**: 신규 자료(스캔본 2종, 자사 약관 2종, 비급여 표준 모델 50만 행)가 입수되어 Phase B·C 내용을 *통합 보험 보상 DB(VectorDB + GraphDB + 관계형 + 객체)* 구축 단위로 재정렬한다. 자세한 자료 특성은 [26_RAW_DOCUMENTS_CATALOG.md](./26_RAW_DOCUMENTS_CATALOG.md) 참조.

| Phase                                                                   | 기간    | 핵심 산출                                                                                                                                                                                     | 마일스톤   | 베타 배포 시점                  |
| ----------------------------------------------------------------------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------- |
| **A. 알파 종료 보정**                                             | 완료    | 출처 첨부 보정, 디버그 로깅, smoke_qa v2 평가, Cloud 로그 보정, UX 보정, 채팅 영속화                                                                                                         | M-α + M-ux | — (완료)                       |
| **B. 베타 1 — 통합 DB 토대 + 메타 강화**                         | 진행 중 | **알파 백업 + 원본 자료 보호 완료**, 비급여 표준 모델(529K 행) → SQLite 적재 완료, 청크 메타 스키마 확장 완료, D3·D4 인덱싱 완료, D6·D7 등록/OCR 대기, 메타 필터·약관 비교 모드 재설계 필요 | M18~M21    | —                              |
| **C. 베타 2 — 스캔본 OCR + GraphDB + VectorDB 병행 + 베타 배포** | 진행 중 | OCR 샘플 검증, PP-Structure/EasyOCR/CLOVA/True Hybrid 경로 구현·비교 일부 완료, D6·D7 전체 OCR 인덱싱·GraphDB 평가·통합 검색 인터페이스 미완료, **챗봇 베타 배포 후보**       | M22~M27    | ✅**이 끝에서 베타 배포** |
| **D. 과제 2 — 영수증·진단서 자동 처리 + 보험금 계산**           | 3~4개월 | 비정형 표 → 데이터프레임 추출, 서류 분류, EDI 매핑(D8 마스터 활용), RAG/Graph ↔ 면·부책 호출, 보험금 계산 룰 엔진                                                                          | M28~M32    | —                              |

(이전 보고서의 Phase E는 9장 "회사 확장 권고"로 이전.)

각 Phase의 외부 의존:

```
Phase A ── 완료 ✅
   │
   ▼
Phase B ── 자사 약관 D3·D4 입수·인덱싱 완료 ✅ / 비급여 표준 모델 D8 SQLite 적재 완료 ✅
   │      D5 원본 부재 / D6·D7 OCR 필요 / 100개+ 추가 약관 입수는 점진적
   │
   ▼
Phase C ── 스캔본 D6·D7 입수 완료 ✅ / OCR 샘플 검증·True Hybrid 경로 구현 진행 ✅
   │      D6·D7 전체 OCR 인덱싱 미완료 / (게이트) GraphDB 도입 평가 결과
   │
   ▼  ── 챗봇 베타 배포
   │
Phase D ── EDI 비급여 표준 모델 D8 활용 ✅
           비급여 사업자번호 매핑 데이터 의존
           Phase C에서 베타 안정 운영 중일 것
```

### 5.1 Phase A 상세 (알파 종료 보정, ~2주)

- [x] 출처 첨부를 "실제 답변에서 사용된 근거" 중심으로 좁히기
- [x] Dense/BM25/RRF/Rerank 단계별 디버그 로그 및 관리자 검색 진단 탭
- [x] smoke_qa v2 — 약관 정형 모드 문항 추가
- [x] Cloud 로그 노이즈 축소
- [x] UX 보정 — 검색 진단 상시 수집, Markdown 물결표 보정, 계정별 채팅 영속화와 멀티 채팅 사이드바

### 5.2 Phase B 상세 (베타 1 — 다중 약관 메타 강화, 1~2개월)

- **M18** — 메타 스키마 확장: `insurance_company`, `is_own_company`, `product_*`, `effective_date`, `version`, `coverage_category`, `clause_type`, `content_type`, `source_method`, `confidence`, `bbox`, `linked_std_cds` 추가. **완료**
- **M19** — 자동 인덱싱 파이프라인: 기본 `scripts/ingest.py`가 D3·D4 신규 약관 인덱싱과 `requires_ocr` 기본 제외를 처리. `scripts/ingest_batch.py` 형태의 다중 폴더 배치 인제스트는 아직 미완료
- **M20** — 사이드바 필터 보강: 자사·타사/상품 필터 1차 구현 후 Cloud 원본 파일 부재 이슈로 제거. Chroma 메타 기반 필터로 재도입 필요
- **M21** — 약관 비교 모드: 동일 보장종목 N개 회사 비교 표는 미구현. M20 필터 안정화 후 착수

### 5.2.1 Phase B 단계 0 — 통합 DB 토대 만들기 (M18 직전, 첫 단계 명세)

본 토대 작업은 [25_BETA_DEVELOPER_PROMPT.md](./25_BETA_DEVELOPER_PROMPT.md)에 첫 단계 명세로 별도 작성. 핵심 산출:

- **알파 백업** (롤백 가능 상태 확보, 외부 디렉토리) — 완료
- **원본 자료 GitHub 푸시 금지 안전망** (`.gitignore` + staged 원본 차단 스크립트) — 완료
- **데이터 디렉토리 표준화** (`data/raw/`, `data/extracted/`, `data/index/{graph,relational}/`) — 완료
- **비급여 표준 모델(D8) → SQLite 적재** (`nonpay_standard` 테이블, 527,679 유니크 `std_cd` 행) — 완료
- **청크 메타 스키마 확장** (옵션 필드 추가) — 완료
- **신규 자료(D3·D4·D6·D7) 등록** — 완료
- **D3·D4 실제 인덱싱** — 완료, 전체 청크 4,925개 기준으로 재생성

### 5.3 Phase C 상세 (베타 2 — 스캔본 OCR + GraphDB·VectorDB 병행 + 베타 배포, 2~3개월)

- **M22** — OCR/Vision 파이프라인 (스캔본 D6·D7 대상, 진행 중)
  - 1차: PaddleOCR + PP-Structure로 텍스트·표·이미지 영역 분리 (한국어, 무료)
  - 2차(폴백): EasyOCR 또는 Naver CLOVA OCR. 샘플 검증에서 EasyOCR은 본문 OCR 후보로 양호했고, CLOVA native table은 p066 표 구조 반환을 확인
  - 3차(옵션): 표·도식 이해를 위한 Vision LLM 기반 표 정리/캡션 자동 생성
  - 노이즈 제거: 밑줄·필기·반복 머리/바닥글은 stroke·반복 패턴 휴리스틱으로 제거 후 메타에 카운트만 보존
  - 산출 표준: `data/extracted/<doc_id>/{text,tables,images}/manifest.json`
- **M23** — 다중 콘텐츠 청크 인덱싱(VectorDB, 부분 완료)
  - `content_type ∈ {text, table, image, formula}`, `source_method`, `confidence`, `bbox` 메타 적용
  - 표는 JSON(헤더+행) + 직렬화 텍스트 청크 + 원본 PNG 동시 저장
  - 이미지는 파일 보관 + 캡션 청크만 검색
  - 기존 알파 인덱스(D1·D2·D5)도 동일 스키마로 마이그레이션 (`migration_map.json`로 청크 ID 호환)
- **M24** — 관계형 DB 통합 검색
  - `nonpay_standard`(D8)와 청크 메타 `linked_std_cds` 양방향 lookup
  - 답변에 표준코드 매칭 시 자동으로 `std_cd_nm`·`hira_care_type_cd_nm`·`pay_opn_cd_nm` 카드 노출
- **M25** — GraphDB 도입 평가 + (필요 시) 본격 구축
  - 4.3 신호표 기준 정량 평가 → ADR 문서화
  - 도입 권장 시: 보험사·상품·약관·조항·보장종목·표준코드·면책 노드와 보상함·보상안함·정의함·해당함·면책함 엣지로 **본격 그래프** 구축
  - PoC 도구는 NetworkX 인메모리 → 권장 시 Neo4j Community(Docker) 본격 도입
  - VectorDB·BM25·GraphDB 결과를 RRF에 합산하는 hybrid retriever 구현
- **M26** — 환각 방지 강화
  - 출처 강제(`chunk_id` 또는 `clause_id` 인용 없으면 답변 차단·재생성)
  - 결정론적 판정은 GraphDB 결과 우선, LLM은 자연어 정리 담당
  - 환각률 측정 메트릭(`hallucination_rate = 1 - (citations_resolved / citations_claimed)`)을 평가에 추가
- **M27** — 약관 비교 모드 + 베타 배포
  - 동일 보장종목·표준코드를 자사·타사 N개 약관에서 동시 검색·비교
  - **챗봇 베타 정식 배포** (사내망 또는 Streamlit Cloud — 보안 정책 결정 따라)

### 5.4 Phase D 상세 (과제 2 — 영수증 처리, 3~4개월)

- **M28** — 서류 입력·분류: 다중 파일 업로드, 자동 분류기(영수증/진단서/세부내역서). 룰 + LLM 보조 분류
- **M29** — **비정형 표 → 데이터프레임 추출 (핵심 모듈)**: 병원마다 다른 표 형식을 정형 데이터프레임으로 변환
  - 1차: pdfplumber·Camelot·Tabula로 텍스트형 표 추출
  - 2차: PaddleOCR PP-Structure(table)로 스캔/이미지 표 인식
  - 3차: layout-aware 후처리 — 헤더 자동 매칭(수가코드/항목명/단가/수량/금액/본인부담금/공단부담금 등 표준 컬럼), 합계 검산, 누락 행 감지
  - 결과: pandas DataFrame + 표준 컬럼 스키마 + 신뢰도 점수
  - 검증: 추출된 합계가 영수증 총액과 ±1원 이내 일치하는지 자동 체크
- **M30** — EDI 매핑 + 비급여 매핑: D8 비급여 표준 모델(SQLite, Phase B에서 적재 완료)로 lookup. 비급여는 사업자번호 + 자체 코드 → 표준 매핑 테이블
- **M31** — RAG/Graph ↔ 면·부책 호출: 추출된 코드·보장종목을 입력으로 약관 정형 검색(보상 판정 모드) 또는 GraphDB 쿼리를 내부 호출. 답변을 면·부책 라벨로 정규화
- **M32** — 보험금 계산 룰 엔진 + 보고서: 공제·한도·중복 보장 차감 로직, 결과 보고서(고객용·사내용 PDF), 단건·배치 처리 두 모드

---

## 6. 통합 플랫폼 아키텍처 스케치 (베타 종료 시점, 캡스톤 산출)

```
                ┌──────────────────────────────────────────────────┐
                │  신한EZ 사내 보험 업무 플랫폼 (단일 Streamlit)           │
                │  · 사내 직원 전용                                    │
                │  · 단일 인증 (역할: employee / admin)                │
                └────────┬───────────────────────────┬──────────────┘
                         │                           │
                ┌────────▼────────┐         ┌────────▼─────────┐
                │  과제 1 — 챗봇    │         │ 과제 2 — 보험금     │
                │  (Phase A~C)    │         │   자동 계산        │
                │ · 자연어 질의      │         │  (Phase D)       │
                │ · 약관 정형 폼     │         │ · 서류 업로드       │
                │ · 퀵 코드         │         │ · 표→DataFrame    │
                │ · 약관 비교 모드   │         │ · 보험금 계산       │
                │ · 관리자 대시보드   │         │ · 보고서           │
                └────────┬────────┘         └────────┬─────────┘
                         │                           │
                         └────────────┬──────────────┘
                                      │
                ┌─────────────────────▼─────────────────────┐
                │       공통 판단 엔진 (Decision Core)       │
                │   · 약관 RAG (Dense + BM25 + Rerank)      │
                │   · GraphRAG PoC (Phase C 도입 시)         │
                │   · 메타 라우팅 (자사/타사·상품·시행일)     │
                └────────┬─────────────────────────┬────────┘
                         │                         │
              ┌──────────▼──────────┐    ┌─────────▼─────────┐
              │ 약관 인덱스           │    │ 코드/매핑 DB        │
              │ · BGE-M3 + Chroma    │    │ · EDI 표준 50만행   │
              │ · BM25               │    │ · 비급여 매핑       │
              │ · 다중 콘텐츠 메타    │    │ · KCD/ICD-10        │
              └──────────────────────┘    └─────────────────────┘
                         ▲                           ▲
              ┌──────────┴───────────────────────────┴──────┐
              │   문서 입수 + OCR 파이프라인                  │
              │   · 네이티브 PDF 텍스트 추출                  │
              │   · PaddleOCR + PP-Structure (약관)          │
              │   · CLOVA OCR (영수증·진단서, Phase D)        │
              │   · 비정형 표 → DataFrame 정규화 (Phase D)    │
              └──────────────────────────────────────────────┘

                ┌──────────────────────────────────────────────┐
                │  공통 인프라: 사용자/인증/로그/LLM 추상화       │
                │  · users.json + 역할(employee/admin)          │
                │  · 사용자별 감사 로그                          │
                │  · LLM 팩토리 (Ollama 로컬 / OpenAI 클라우드)  │
                └──────────────────────────────────────────────┘
```

캡스톤 범위에서는 단일 Streamlit 앱·단일 인증·단일 로그 시스템으로 묶는다. 마이크로서비스 분리는 회사 확장 단계.

---

## 7. 권장 의사결정 항목 (사용자 결정 대기)

다음 단계 진입 전 결정 필요:

1. **메타 필터 재도입 방식** — 파일 존재 여부가 아니라 Chroma/청크 메타 기준으로 자사·타사·상품 필터를 복구할지 결정
2. **타사 약관 표시 정책** — 베타 UI에서 타사 약관 검색 결과를 자사와 동일하게 노출할지, 별도 표시할지
3. **OCR 운영 도구 조합** — PP-Structure + EasyOCR 무료 조합으로 갈지, CLOVA native table을 약관 OCR에도 사용할지 결정
4. **D6·D7 원문/추출물 클라우드 정책** — `실무가이드`는 사내 자료 가능성이 있어 Cloud 배포 제외 유지 여부 확인
5. **GraphDB 도입 평가 시점** — Phase C 안에서 ADR 작성(권장) vs Phase D와 병행
6. **D8 관계형 DB 노출 범위** — 비급여 표준 모델 SQLite를 챗 UI 카드/검색 필터에 어느 수준까지 연결할지 결정
7. **베타 배포 환경** — 현재 Streamlit Community Cloud 유지 vs 사내망 이전(보안 정책 의존)
8. **D5 보상가이드북 원본 확보** — 로컬 원본 파일 부재 상태를 해소할지, 범위에서 제외할지 결정

---

## 8. 캡스톤 범위 종료 기준

다음을 모두 충족하면 캡스톤 산출이 완료되었다고 판단한다.

- [x] 알파 종료 보정 완료 (Phase A)
- [x] 사용자별 채팅 영속화와 멀티 채팅 사이드바
- [x] 비급여 표준 모델 SQLite 적재와 원본 자료 GitHub 푸시 방지
- [x] D3·D4 자사 약관 등록·인덱싱
- [ ] 100개+ 약관 검색이 가능한 메타 강화 RAG (Phase B)
- [ ] 메타 기반 자사·타사/상품 필터와 약관 비교 모드
- [ ] 스캔본 약관 OCR 인덱싱 + 다중 콘텐츠(텍스트·표·이미지) 보존 (Phase C M22~M23)
- [ ] GraphDB 도입 필요성 평가 ADR 문서 (Phase C M25, 도입 여부 무관)
- [ ] **챗봇 베타 배포** (Phase C 종료)
- [ ] 비정형 표 → 데이터프레임 추출 모듈 (Phase D M29)
- [ ] EDI 매핑 + 면·부책 자동 판정 + 보험금 계산 (Phase D M30~M32)
- [ ] 통합 플랫폼 단일 Streamlit 앱에서 두 과제 모두 사용 가능
- [x] 현재 회귀 pytest 통과
- [ ] 베타 평가 보고서

---

## 9. 회사 측 정식 운영 확장 권고 (캡스톤 범위 외)

캡스톤 종료 후 신한EZ 측에서 정식 운영으로 이어가려 할 때 권장되는 확장 항목. 캡스톤 팀은 본 항목을 수행하지 않고 인수인계 문서로만 정리한다.

| 항목                | 내용                                                                                        |
| ------------------- | ------------------------------------------------------------------------------------------- |
| 마이크로서비스 분리 | 옵션 C — RAG·OCR/계산·인증·UI 각각 서비스로 분리, API gateway                           |
| 자체 GPU 인스턴스   | 사내 GPU(예: A6000·H100) 도입으로 exaone3.5 또는 더 큰 한국어 모델 운영, OpenAI 의존 축소  |
| GraphRAG 본격 운영  | (캡스톤에서 PoC만 했다면) Neo4j 클러스터로 100개+ 약관 전체 그래프, 다중 hop 추론 본격 활용 |
| 외부 객체 저장소    | 로그·인덱스·자산을 S3/R2/GCS에 영속화 (현재는 ephemeral)                                  |
| 운영 모니터링       | Prometheus + Grafana, 알림(Slack), 비용·성능 대시보드                                      |
| 사용자 관리 자동화  | SSO/OAuth 연동(사내 IdP), 사용자 라이프사이클 관리                                          |
| 비밀 관리           | Vault·AWS Secrets Manager 도입                                                             |
| CI/CD               | GitHub Actions/Jenkins 빌드·테스트·배포 파이프라인                                        |
| 보안·감사          | 외부 감사 로그 영속화, 접근 통제 강화, 데이터 분류·라벨링                                  |
| 약관 자동 수집      | 외부 공시 약관 크롤링·정기 갱신                                                            |

이 항목들은 모두 **신한EZ 정식 운영팀의 의사결정·예산이 필요한 작업**이며, 캡스톤 산출에 의존하지 않고도 도입 가능한 별도 트랙이다.

---

## 10. 결론 (한 줄 요약)

> **알파 종료 보정은 완료되었고, 현재 프로젝트는 베타 Stage 2 이후 OCR/True Hybrid 검증 단계에 있다.** 다음 핵심은 (1) 메타 기반 필터와 약관 비교 모드 재도입, (2) D6·D7 스캔본 전체 OCR 인덱싱, (3) D8 관계형 DB와 RAG 답변 연결, (4) GraphDB 도입 필요성 ADR 작성이다. 이 네 항목을 마무리해 챗봇 베타를 안정화한 뒤, 과제 2(보험금 자동 계산, Phase D)를 같은 플랫폼 위에 얹는다.
