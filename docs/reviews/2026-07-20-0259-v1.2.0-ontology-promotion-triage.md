# v1.2.0 일반 질의 온톨로지 승격 Triage

- Timestamp: 2026-07-20 02:59:18 +0900
- Cycle: v1.2.0-general-ontology-promotion-20260720-0259
- Project root: `/Users/june_kim/Projects/insurance-rag-chatbot`
- Developer thread: `019eaf4a-6338-7812-bf3b-663df7d83d4f`
- Review Team thread: `019ecf26-a373-7bf2-bc0a-62c13deb349f`
- Scope/spec: 검토가 끝난 일반 질의 온톨로지 v1.2.0 변경의 커밋, DGX 보호 메인 반영, safe-baseline 활성화, 검증 및 `origin/master` 푸시

## Reported

- Developer는 내부 Graph manifest에만 존재하는 예기치 않은 키를 fail-closed로 거부하도록 최소 수정했다고 보고했다.
- 신규 회귀 테스트는 수정 전 실패하고 수정 후 통과했으며, focused Python `158 passed`, 전체 Python `1130 passed`, Node `45 passed`, 프런트엔드 빌드 및 문법 검사 통과를 보고했다.
- 권위 후보는 `/srv/shared/workspaces/muldae/ontology-v1.2.0-candidate-release-20260719-attempt4/v1.2.0-general-ontology`이며 재빌드하거나 변경하지 않았다고 보고했다.

## Observed

- Review Team은 별도 lock 및 SQLite 경로로 전체 Python `1130 passed, 3 warnings`, Node `45 passed`, 프런트엔드 빌드와 `node --check` 통과를 독립 확인했다.
- Review Team은 외부에만 있는 키, 내부에만 있는 키, 기존 키의 값 불일치가 각각 deterministic하게 거부됨을 확인했다.
- 실제 attempt4 후보의 내부/외부 manifest는 `17/17` 동일하고 누락·예상외·값 불일치가 모두 0이다.
- Graph DB SHA-256은 `2b39c60cd5f8f9d936021a2bb2e1707928870719943cfad7932f81efa7aca9eb`이며, ontology content hash는 `ffad858ea1339bf5196a5445395aed5003a6344990f7ae32c0ef3a039e33861b`, provenance content hash는 `bf6e6df6d458f1f896103b428c3b302d71ca658c24d046f5d4dccc24aa2fecf7`로 유지됐다.
- Review Team 최종 판정은 `REVIEW_TEAM_V1_2_0_MANIFEST_KEYSET_FIXBACK_PASS`이다.
- 사용자 요청은 이상이 없으면 두 번째 자리 수를 올린 `v1.2.0`으로 푸시하는 명시적 승격 권한을 포함한다.

## Not Verified

- 검토된 변경은 아직 Git 커밋으로 고정되지 않았다.
- DGX 보호 메인에는 아직 반영되지 않았고, 새 safe-baseline은 운영 API에 아직 활성화되지 않았다.
- 활성화 후 실제 프로세스가 새 runtime root·Graph snapshot을 읽는지, 기존 SGLang Qwen3 Next 80B 모델이 유지되는지는 아직 확인하지 않았다.
- 최종 실무자 UAT 워크북과 릴리스 태그는 구현 커밋 확정 뒤 메인 책임자가 생성할 예정이다.

## Findings

- 승격 차단 결함 없음.
- 공용 `/tmp/insurance-rag-ontology-rebuild.lock`의 기존 소유권 충돌은 별도 lock 사용 시 재현되지 않는 환경 잔여물이며 제품 결함으로 분류하지 않는다.
- 계산 룰, rule link, processing policy는 범위 밖이며 활성화 전후 해시 동결 검증이 필수다.

## Decision

`DEVELOPER_FIXBACK`

이 결정명은 구현 결함 수정이 아니라, Review PASS 뒤 별도 권한으로 실행하는 승격 작업의 Developer 소유를 뜻한다.

## Dispatch

- Target: Developer thread `019eaf4a-6338-7812-bf3b-663df7d83d4f`
- 지시 요약:
  1. 격리 workspace에서 검토된 v1.2.0 변경만 정확히 스테이징해 `feat(ontology): rebuild general-query graph baseline` 커밋을 만든다.
  2. DGX 보호 메인 `3cd6617` clean 상태를 재확인한 뒤 그 커밋만 cherry-pick하고 전체 회귀를 재검증한다.
  3. attempt4 후보 해시를 다시 확인한 뒤 `/srv/ai-ops/runtime/insurance-rag-chatbot/safe-baseline-v1.2.0`에 불변 복사하고 검증한다.
  4. secret 값을 출력하지 않고 offline env를 백업·원자 수정하여 safe-baseline root와 Graph snapshot 경로만 지정한다.
  5. LLM 전환 없이 API-only 재시작하고 상태, 모델, Graph, 55개 승인 개념, 17개 alias, 계산 룰 해시 불변을 확인한다.
  6. 모든 검증이 통과할 때만 `origin/master`에 푸시한다. 실패하면 env를 복구하고 API-only rollback 후 푸시하지 않는다.
  7. `v1.2.0` 태그는 최종 UAT 문서 커밋 뒤 메인 책임자가 생성하므로 이 단계에서는 만들지 않는다.
